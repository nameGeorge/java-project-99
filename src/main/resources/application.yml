rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem
spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      generate-ddl: true
  output:
    ansi:
      enabled: always
  profiles:
    default: dev